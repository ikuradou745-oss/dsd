{
  "name": "forcord",
  "version": "1.0.0",
  "description": "WebRTC対応の超高音質リアルタイムボイスチャット＆メッセージングアプリ",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3000",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 3000"
  },
  "dependencies": {},
  "devDependencies": {
    "vite": "^8.2.2"
  }
}
